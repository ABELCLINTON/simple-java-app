pipeline {
    agent any

    tools {
        maven 'maven-3'
    }

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url:'https://github.com/ABELCLINTON/simple-java-app.git'
            }
        }
    }
}
